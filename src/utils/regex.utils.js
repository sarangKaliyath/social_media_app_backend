module.exports  = {
    // this will all letters and numbers;
    "lettersAndNumbers" : /^[a-zA-Z0-9]+$/,
    
    // this will allow a-z, A-Z, 0-9, _, #, &;
    "validUsernames": /^[a-zA-Z0-9_#&]+$/
}